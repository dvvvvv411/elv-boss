

## Änderung
In `vite.config.ts` den Eintrag in `server.allowedHosts` von `panel.antpiregmbh.de` auf `panel.piana-heizoel.de` ändern.

| Datei | Änderung |
|---|---|
| `vite.config.ts` | `allowedHosts: ["panel.piana-heizoel.de"]` |

